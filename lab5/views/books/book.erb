<div class="grid-x">
  <div class="cell large-12">
    <h3>Информация о книге</h3>

    <p>Название: <%= @book.name %></p>
    <p>Автор: <%= @book.author %></p>
    <p>Жанр: <%= @book.genre %></p> 
    <p>Цена: <%= @book.price %></p>
    <p>Количество штук в магазине: <%= @book.quantity %></p>
    <a class="alert button" href="<%= path(@book, 'delete') %>">Удалить</a>
    <a class="button secondary" href="<%= books_path %>">Назад</a>
    <a class="button" href="<%= path(@book, 'add') %>">Добавить в список покупок</a>
  </div>
</div>
