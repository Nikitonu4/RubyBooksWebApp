<div class="grid-x">
  <div class="cell large-12">
    <h3>Информация о книге</h3>

    <p>Название: <%= @book.title %></p>
    <p>Автор: <%= @book.author %></p>
    <p>Жанр: <%= @book.genre %></p> 
    <p>Цена: <%= @book.price %></p>
    <p>Количество штук в магазине: <%= @book.quantity %></p>
    <a class="alert button" href="<%= path(@book, 'delete') %>">Удалить</a>
    <a class="button secondary" href="<%= books_path %>">Книги</a>
     <a class="button" href="<%= shop_path %>">Категории</a>
  </div>
</div>
